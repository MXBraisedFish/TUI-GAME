# utf8 库

## 基本库说明

`utf8` 提供 UTF-8 字符串处理：字符计数、码点转换、ASCII 互转与位置定位。游戏与屏保会话均可使用。库表为只读。

## 目录

### 常量

本库无常量。

### 方法

| 方法名                        | 说明                       |
| ----------------------------- | -------------------------- |
| `utf8.len(text)`              | 返回字符数                 |
| `utf8.byte_len(text)`         | 返回字节数                 |
| `utf8.is_ascii(text)`         | 判断是否全为 ASCII         |
| `utf8.codepoint_to_char{...}` | 码点序列转字符串           |
| `utf8.ascii_to_char{...}`     | ASCII 码序列转字符串       |
| `utf8.char_to_codepoint{...}` | 字符转码点                 |
| `utf8.char_to_ascii{...}`     | 字符转 ASCII 码            |
| `utf8.char_position{...}`     | 定位第 N 个字符的字节位置  |
| `utf8.codepoints(text)`       | 遍历全部码点               |
| `utf8.next{...}`              | 返回指定位置后的下一个字符 |

## 方法

### `len`

- **方法作用**：返回字符串的字符数（Unicode 标量数量）。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型   | 必填 | 默认值 | 说明       | 额外补充         |
| ------ | ------ | ---- | ------ | ---------- | ---------------- |
| `text` | string | 是   | —      | 目标字符串 | 必须为合法 UTF-8 |

- **方法返回**：

| 返回值名 | 类型    | 说明   | 额外补充        |
| -------- | ------- | ------ | --------------- |
| `count`  | integer | 字符数 | 中文算 1 个字符 |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `utf8.len`

---

### `byte_len`

- **方法作用**：返回字符串的字节数。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型   | 必填 | 默认值 | 说明       | 额外补充         |
| ------ | ------ | ---- | ------ | ---------- | ---------------- |
| `text` | string | 是   | —      | 目标字符串 | 必须为合法 UTF-8 |

- **方法返回**：

| 返回值名 | 类型    | 说明   | 额外补充          |
| -------- | ------- | ------ | ----------------- |
| `bytes`  | integer | 字节数 | 中文通常为 3 字节 |

- **方法的使用**：

```lua

```

---

### `is_ascii`

- **方法作用**：判断字符串是否全部为 ASCII 字符。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型   | 必填 | 默认值 | 说明       | 额外补充 |
| ------ | ------ | ---- | ------ | ---------- | -------- |
| `text` | string | 是   | —      | 目标字符串 | —        |

- **方法返回**：

| 返回值名 | 类型    | 说明           | 额外补充            |
| -------- | ------- | -------------- | ------------------- |
| `ascii`  | boolean | 是否全为 ASCII | 空字符串返回 `true` |

- **方法的使用**：

```lua

```

---

### `codepoint_to_char`

- **方法作用**：将一组 Unicode 码点转换为字符串。
- **方法要求**：无
- **方法参数**：

| 参数名   | 类型  | 必填 | 默认值 | 说明     | 额外补充                      |
| -------- | ----- | ---- | ------ | -------- | ----------------------------- |
| `values` | table | 是   | —      | 码点数组 | 每项为合法 Unicode 标量的整数 |

- **方法返回**：

| 返回值名 | 类型   | 说明           | 额外补充     |
| -------- | ------ | -------------- | ------------ |
| `text`   | string | 拼接后的字符串 | 非法码点抛错 |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `utf8.char`（本实现以数组表传入）

---

### `ascii_to_char`

- **方法作用**：将一组 ASCII 码转换为字符串（仅 `0..127`）。
- **方法要求**：无
- **方法参数**：

| 参数名   | 类型  | 必填 | 默认值 | 说明         | 额外补充              |
| -------- | ----- | ---- | ------ | ------------ | --------------------- |
| `values` | table | 是   | —      | ASCII 码数组 | 每项必须满足 `0..127` |

- **方法返回**：

| 返回值名 | 类型   | 说明           | 额外补充     |
| -------- | ------ | -------------- | ------------ |
| `text`   | string | 拼接后的字符串 | 越界码值抛错 |

- **方法的使用**：

```lua

```

---

### `char_to_codepoint`

- **方法作用**：将字符串指定区间的字符转换为码点序列。
- **方法要求**：无
- **方法参数**：

| 参数名   | 类型    | 必填 | 默认值  | 说明         | 额外补充                    |
| -------- | ------- | ---- | ------- | ------------ | --------------------------- |
| `text`   | string  | 是   | —       | 目标字符串   | —                           |
| `start`  | integer | 否   | `1`     | 起始字符位置 | 1 起始，负数从末尾计算      |
| `finish` | integer | 否   | `start` | 结束字符位置 | 默认仅转换 `start` 处的字符 |

- **方法返回**：

| 返回值名 | 类型         | 说明           | 额外补充       |
| -------- | ------------ | -------------- | -------------- |
| `...`    | 若干 integer | 每个字符的码点 | 越界区间返回空 |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `utf8.codepoint`

---

### `char_to_ascii`

- **方法作用**：将字符串指定区间的字符转换为 ASCII 码序列；非 ASCII 字符对应位置返回 `nil`。
- **方法要求**：无
- **方法参数**：

| 参数名   | 类型    | 必填 | 默认值  | 说明         | 额外补充                    |
| -------- | ------- | ---- | ------- | ------------ | --------------------------- |
| `text`   | string  | 是   | —       | 目标字符串   | —                           |
| `start`  | integer | 否   | `1`     | 起始字符位置 | 1 起始，负数从末尾计算      |
| `finish` | integer | 否   | `start` | 结束字符位置 | 默认仅转换 `start` 处的字符 |

- **方法返回**：

| 返回值名 | 类型                | 说明                | 额外补充                |
| -------- | ------------------- | ------------------- | ----------------------- |
| `...`    | 若干 integer 或 nil | 每个字符的 ASCII 码 | 非 ASCII 字符返回 `nil` |

- **方法的使用**：

```lua

```

---

### `char_position`

- **方法作用**：定位文本中第 N 个字符的 1 起始字节位置。
- **方法要求**：无
- **方法参数**：

| 参数名  | 类型    | 必填 | 默认值 | 说明             | 额外补充             |
| ------- | ------- | ---- | ------ | ---------------- | -------------------- |
| `text`  | string  | 是   | —      | 目标字符串       | —                    |
| `index` | integer | 是   | —      | 相对偏移的字符数 | 可为负（从末尾计算） |
| `start` | integer | 否   | `1`    | 起始字符位置     | 1 起始               |

- **方法返回**：

| 返回值名   | 类型    | 说明                         | 额外补充           |
| ---------- | ------- | ---------------------------- | ------------------ |
| `position` | integer | 目标字符的字节位置（1 起始） | 超出范围返回 `nil` |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `utf8.offset`

---

### `codepoints`

- **方法作用**：返回遍历字符串全部字符的迭代函数，每次产出 `position, codepoint`。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型   | 必填 | 默认值 | 说明       | 额外补充 |
| ------ | ------ | ---- | ------ | ---------- | -------- |
| `text` | string | 是   | —      | 目标字符串 | —        |

- **方法返回**：

| 返回值名   | 类型     | 说明     | 额外补充                       |
| ---------- | -------- | -------- | ------------------------------ |
| `iterator` | function | 迭代函数 | 每次返回 `position, codepoint` |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `utf8.codes`

---

### `next`

- **方法作用**：返回指定字节位置之后的第一个字符及其位置。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型    | 必填 | 默认值 | 说明         | 额外补充                      |
| ------ | ------- | ---- | ------ | ------------ | ----------------------------- |
| `text` | string  | 是   | —      | 目标字符串   | —                             |
| `pos`  | integer | 是   | —      | 起始字节位置 | 位置为该字符的 1 起始字节偏移 |

- **方法返回**：

| 返回值名    | 类型    | 说明                       | 额外补充               |
| ----------- | ------- | -------------------------- | ---------------------- |
| `codepoint` | integer | 下一个字符的码点           | 无下一个字符时为 `nil` |
| `position`  | integer | 该字符的字节位置（1 起始） | 无下一个字符时为 `nil` |

- **方法的使用**：

```lua

```
