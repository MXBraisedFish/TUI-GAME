# file 库

## 基本库说明

`file` 提供异步文件读写与目录枚举。**读操作**无限制；**写与枚举**仅游戏会话且关闭安全模式时可用。库表为只读。

> 所有方法均为**异步**：调用后不直接返回结果，宿主完成后通过类型为 `file` 的事件回调（`HandleEvent`）返回，事件 `data` 中含 `request_id`、`ok` 与结果字段或 `error`。所有路径均以 `assets/` 目录为基准、必须为相对路径，禁止越出根目录。

## 目录

### 常量

| 常量名 | 说明 |
| ------ | ---- |
| `file.AUTO` | 编码/换行：自动检测 |
| `file.ALL` | 换行：全部统一 |
| `file.CR` | 换行：`\r` |
| `file.LF` | 换行：`\n` |
| `file.CRLF` | 换行：`\r\n` |
| `file.UTF_8` / `UTF_16LE` / `UTF_16BE` | 编码：UTF 系列 |
| `file.GBK` / `GB18030` | 编码：简体中文系列 |
| `file.BIG5` | 编码：繁体中文 |
| `file.SHIFT_JIS` / `EUC_JP` / `ISO_2022_JP` | 编码：日文系列 |
| `file.EUC_KR` | 编码：韩文 |
| `file.WINDOWS_874` / `WINDOWS_1250`~`WINDOWS_1258` | 编码：Windows 代码页 |
| `file.ISO_8859_2`~`ISO_8859_16` / `ISO_8859_8_I` | 编码：ISO-8859 系列 |
| `file.KOI8_R` / `KOI8_U` | 编码：KOI8 系列 |
| `file.IBM866` | 编码：俄文 IBM866 |
| `file.MACINTOSH` | 编码：Macintosh |
| `file.X_MAC_CYRILLIC` | 编码：x-mac-cyrillic |

### 方法

| 方法名 | 说明 |
| ------ | ---- |
| `file.read{...}` | 读取文本文件 |
| `file.write{...}` | 写入文本文件 |
| `file.list_dir{...}` | 枚举目录 |

## 常量

### `AUTO`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding`、`end_of_line` |
| **作用** | 自动检测编码或换行符 |
| **额外补充** | 值为字符串 `"auto"` |

---

### `ALL`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `end_of_line` |
| **作用** | 将文本中所有换行符统一 |
| **额外补充** | 值为字符串 `"all"` |

---

### `CR`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `end_of_line` |
| **作用** | 换行符为 `\r`（老式 Mac） |
| **额外补充** | 值为字符串 `"cr"` |

---

### `LF`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `end_of_line` |
| **作用** | 换行符为 `\n`（Unix/Linux） |
| **额外补充** | 值为字符串 `"lf"` |

---

### `CRLF`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `end_of_line` |
| **作用** | 换行符为 `\r\n`（Windows） |
| **额外补充** | 值为字符串 `"crlf"` |

---

### `UTF_8`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 UTF-8 编码 |
| **额外补充** | 值为字符串 `"utf-8"` |

---

### `UTF_16LE`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 UTF-16 小端编码 |
| **额外补充** | 值为字符串 `"utf-16le"` |

---

### `UTF_16BE`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 UTF-16 大端编码 |
| **额外补充** | 值为字符串 `"utf-16be"` |

---

### `GBK`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 GBK 编码 |
| **额外补充** | 值为字符串 `"gbk"` |

---

### `GB18030`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 GB18030 编码 |
| **额外补充** | 值为字符串 `"gb18030"` |

---

### `BIG5`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Big5 编码 |
| **额外补充** | 值为字符串 `"big5"` |

---

### `SHIFT_JIS`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Shift-JIS 编码 |
| **额外补充** | 值为字符串 `"shift_jis"` |

---

### `EUC_JP`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 EUC-JP 编码 |
| **额外补充** | 值为字符串 `"euc-jp"` |

---

### `ISO_2022_JP`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-2022-JP 编码 |
| **额外补充** | 值为字符串 `"iso-2022-jp"` |

---

### `EUC_KR`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 EUC-KR 编码 |
| **额外补充** | 值为字符串 `"euc-kr"` |

---

### `WINDOWS_874`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Windows-874 编码（泰文） |
| **额外补充** | 值为字符串 `"windows-874"` |

---

### `WINDOWS_1250`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Windows-1250 编码（中欧） |
| **额外补充** | 值为字符串 `"windows-1250"` |

---

### `WINDOWS_1251`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Windows-1251 编码（西里尔） |
| **额外补充** | 值为字符串 `"windows-1251"` |

---

### `WINDOWS_1252`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Windows-1252 编码（西欧） |
| **额外补充** | 值为字符串 `"windows-1252"` |

---

### `WINDOWS_1253`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Windows-1253 编码（希腊） |
| **额外补充** | 值为字符串 `"windows-1253"` |

---

### `WINDOWS_1254`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Windows-1254 编码（土耳其） |
| **额外补充** | 值为字符串 `"windows-1254"` |

---

### `WINDOWS_1255`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Windows-1255 编码（希伯来） |
| **额外补充** | 值为字符串 `"windows-1255"` |

---

### `WINDOWS_1256`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Windows-1256 编码（阿拉伯） |
| **额外补充** | 值为字符串 `"windows-1256"` |

---

### `WINDOWS_1257`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Windows-1257 编码（波罗的） |
| **额外补充** | 值为字符串 `"windows-1257"` |

---

### `WINDOWS_1258`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Windows-1258 编码（越南） |
| **额外补充** | 值为字符串 `"windows-1258"` |

---

### `ISO_8859_2`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-2 编码（中欧） |
| **额外补充** | 值为字符串 `"iso-8859-2"` |

---

### `ISO_8859_3`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-3 编码（南欧） |
| **额外补充** | 值为字符串 `"iso-8859-3"` |

---

### `ISO_8859_4`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-4 编码（北欧） |
| **额外补充** | 值为字符串 `"iso-8859-4"` |

---

### `ISO_8859_5`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-5 编码（西里尔） |
| **额外补充** | 值为字符串 `"iso-8859-5"` |

---

### `ISO_8859_6`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-6 编码（阿拉伯） |
| **额外补充** | 值为字符串 `"iso-8859-6"` |

---

### `ISO_8859_7`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-7 编码（希腊） |
| **额外补充** | 值为字符串 `"iso-8859-7"` |

---

### `ISO_8859_8`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-8 编码（希伯来） |
| **额外补充** | 值为字符串 `"iso-8859-8"` |

---

### `ISO_8859_8_I`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-8-I 编码 |
| **额外补充** | 值为字符串 `"iso-8859-8-i"` |

---

### `ISO_8859_10`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-10 编码（北欧） |
| **额外补充** | 值为字符串 `"iso-8859-10"` |

---

### `ISO_8859_13`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-13 编码（波罗的） |
| **额外补充** | 值为字符串 `"iso-8859-13"` |

---

### `ISO_8859_14`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-14 编码（凯尔特） |
| **额外补充** | 值为字符串 `"iso-8859-14"` |

---

### `ISO_8859_15`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-15 编码（西欧） |
| **额外补充** | 值为字符串 `"iso-8859-15"` |

---

### `ISO_8859_16`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 ISO-8859-16 编码（东南欧） |
| **额外补充** | 值为字符串 `"iso-8859-16"` |

---

### `KOI8_R`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 KOI8-R 编码（俄文） |
| **额外补充** | 值为字符串 `"koi8-r"` |

---

### `KOI8_U`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 KOI8-U 编码（乌克兰） |
| **额外补充** | 值为字符串 `"koi8-u"` |

---

### `IBM866`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 IBM866 编码（俄文 DOS） |
| **额外补充** | 值为字符串 `"ibm866"` |

---

### `MACINTOSH`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 Macintosh 编码 |
| **额外补充** | 值为字符串 `"macintosh"` |

---

### `X_MAC_CYRILLIC`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | `encoding` |
| **作用** | 使用 x-mac-cyrillic 编码 |
| **额外补充** | 值为字符串 `"x-mac-cyrillic"` |

---

## 方法

### `read`

- **方法作用**：异步读取 `assets/` 目录下的文本文件。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `path` | string | 是 | — | 相对 `assets/` 的文件路径 | 必须为相对路径且存在 |
| `encoding` | const | 否 | `"auto"` | 文本编码 | 使用 `file.*` 编码常量 |
| `end_of_line` | const | 否 | `"auto"` | 换行符规范 | 使用 `file.AUTO/ALL/CR/LF/CRLF` |
| `event_tip` | string | 否 | `nil` | 事件提示文本 | 上限 4 KiB |

- **方法返回**：无直接返回值（异步）。完成后 `HandleEvent` 收到 `file` 事件：

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| `request_id` | integer | 请求标识，用于对应该次调用 |
| `kind` | string | 恒为 `"read_text"` |
| `path` | string | 请求的虚拟路径 |
| `tip` | string / nil | 请求时传入的 `event_tip` |
| `ok` | boolean | 是否成功 |
| `text` | string | 成功时的文本内容（`ok=true`） |
| `error` | table | 失败时含 `code`、`message`（`ok=false`） |

- **方法的使用**：

```lua

```

---

### `write`

- **方法作用**：异步写入文本文件到 `assets/` 目录（允许创建新文件）。
- **方法要求**：仅游戏脚本、关闭安全模式
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `path` | string | 是 | — | 相对 `assets/` 的文件路径 | 必须为相对路径；父目录必须存在 |
| `text` | string | 是 | — | 要写入的文本 | 上限 1 MiB，不含 NUL |
| `encoding` | const | 否 | `"auto"` | 文本编码 | 使用 `file.*` 编码常量 |
| `end_of_line` | const | 否 | `"auto"` | 换行符规范 | 使用 `file.AUTO/ALL/CR/LF/CRLF` |
| `event_tip` | string | 否 | `nil` | 事件提示文本 | 上限 4 KiB |

- **方法返回**：无直接返回值（异步）。完成后 `HandleEvent` 收到 `file` 事件：

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| `request_id` | integer | 请求标识 |
| `kind` | string | 恒为 `"write_text"` |
| `path` | string | 请求的虚拟路径 |
| `tip` | string / nil | 请求时传入的 `event_tip` |
| `ok` | boolean | 是否成功 |
| `error` | table | 失败时含 `code`、`message`（`ok=false`） |

- **方法的使用**：

```lua

```

---

### `list_dir`

- **方法作用**：异步枚举 `assets/` 目录下的条目。
- **方法要求**：仅游戏脚本、关闭安全模式
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `path` | string | 是 | — | 相对 `assets/` 的目录路径 | 必须为相对路径且为目录 |
| `recursive` | boolean | 否 | `false` | 是否递归子目录 | — |
| `file_type` | string | 否 | `nil` | 仅匹配指定扩展名 | 如 `"rs"`；传 `"all"` 或不传匹配全部 |
| `event_tip` | string | 否 | `nil` | 事件提示文本 | 上限 4 KiB |

- **方法返回**：无直接返回值（异步）。完成后 `HandleEvent` 收到 `file` 事件：

| 字段 | 类型 | 说明 |
| ---- | ---- | ---- |
| `request_id` | integer | 请求标识 |
| `kind` | string | 恒为 `"list_dir"` |
| `path` | string | 请求的虚拟路径 |
| `tip` | string / nil | 请求时传入的 `event_tip` |
| `ok` | boolean | 是否成功 |
| `entries` | table | 成功时的条目数组，每项含 `path`、`file_type`（`ok=true`） |
| `error` | table | 失败时含 `code`、`message`（`ok=false`） |

- **方法的使用**：

```lua

```
