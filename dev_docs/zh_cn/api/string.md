# string 库

## 基本库说明

`string` 提供字符串处理：大小写、反转、截取、查找、匹配、替换、格式化，以及独立的**正则**（regex）变体方法。游戏与屏保会话均可使用。库表为只读。

> 模式（pattern）分为两套：**Lua 模式**（`find`/`match`/`gmatch`/`gsub`）与 **Rust 正则**（`regex_find`/`regex_match`/`regex_gmatch`/`regex_gsub`/`regex_test`/`regex_split`）。正则替换串中 `$0..$9` 引用捕获组，`$$` 转义字面 `$`。

## 目录

### 常量

| 常量名 | 说明 |
| ------ | ---- |
| `string.AUTO` | 文本模式：自动 |
| `string.PLAIN_TEXT` | 文本模式：纯文本 |
| `string.RICH_TEXT` | 文本模式：富文本 |

### 方法

| 方法名 | 说明 |
| ------ | ---- |
| `string.lower(text)` | 转小写 |
| `string.upper(text)` | 转大写 |
| `string.reverse(text)` | 反转字符串 |
| `string.regex_escape(text)` | 转义正则特殊字符 |
| `string.sub{...}` | 按字符截取子串 |
| `string.rep{...}` | 重复拼接 |
| `string.find{...}` | 查找首个匹配 |
| `string.match{...}` | 提取匹配捕获 |
| `string.gmatch{...}` | 迭代全部匹配 |
| `string.gsub{...}` | 全局替换 |
| `string.regex_find{...}` | 正则查找 |
| `string.regex_match{...}` | 正则提取 |
| `string.regex_gmatch{...}` | 正则迭代 |
| `string.regex_gsub{...}` | 正则替换 |
| `string.regex_test{...}` | 是否匹配 |
| `string.regex_split{...}` | 正则分割 |
| `string.format{...}` | 格式化字符串 |
| `string.rich_text_to_plain_text{...}` | 富文本转纯文本 |

## 常量

### `AUTO`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | 文本参数 `text_mode` |
| **作用** | 文本模式设为自动：纯文本与富文本均可，不做强制转换 |
| **额外补充** | 值为字符串 `"auto"` |

---

### `PLAIN_TEXT`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | 文本参数 `text_mode` |
| **作用** | 文本模式设为纯文本：以 `f%` 开头的富文本头会被屏蔽为普通字符 |
| **额外补充** | 值为字符串 `"plain_text"` |

---

### `RICH_TEXT`

| 项目 | 内容 |
| ---- | ---- |
| **可应用参数** | 文本参数 `text_mode` |
| **作用** | 文本模式设为富文本：缺失的 `f%` 头会被自动补全 |
| **额外补充** | 值为字符串 `"rich_text"` |

---

## 方法

### `lower`

- **方法作用**：将字符串全部转为小写。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `result` | string | 小写结果 | — |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `string.lower`

---

### `upper`

- **方法作用**：将字符串全部转为大写。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `result` | string | 大写结果 | — |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `string.upper`

---

### `reverse`

- **方法作用**：按字符反转字符串。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `result` | string | 反转结果 | 按 Unicode 字符反转，非按字节 |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `string.reverse`

---

### `regex_escape`

- **方法作用**：转义字符串中的正则特殊字符，使其可作为普通文本参与正则匹配。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `result` | string | 转义后的字符串 | 可直接嵌入正则模式 |

- **方法的使用**：

```lua

```

---

### `sub`

- **方法作用**：按字符位置截取子串。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `start` | integer | 是 | — | 起始字符位置 | 1 起始；负数从末尾计算 |
| `finish` | integer | 否 | 字符串长度 | 结束字符位置 | 负数从末尾计算 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `result` | string | 截取结果 | 越界区间返回空字符串 |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `string.sub`（本实现按字符而非字节）

---

### `rep`

- **方法作用**：将字符串重复 `times` 次，可指定分隔符拼接。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 要重复的字符串 | — |
| `times` | integer | 是 | — | 重复次数 | 必须为非负整数 |
| `sep` | string | 否 | `""` | 相邻副本间的分隔符 | — |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `result` | string | 重复拼接结果 | 结果超 1 MiB 报错 |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `string.rep`

---

### `find`

- **方法作用**：查找首个匹配的位置与捕获；找不到返回 `nil`。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `pattern` | string | 是 | — | Lua 模式 | 长度上限 8 KiB |
| `init` | integer | 否 | `1` | 起始搜索位置 | 不能为 0；负数从末尾计算 |
| `plain` | boolean | 否 | `false` | 是否按纯文本查找 | `true` 时忽略 `pattern` 中的元字符 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `start` | integer | 匹配起点（字符位置，1 起始） | 未匹配时整体返回 `nil` |
| `finish` | integer | 匹配终点（字符位置） | — |
| `captures...` | 若干值 | 各捕获组内容 | 无捕获组时省略 |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `string.find`

---

### `match`

- **方法作用**：提取首个匹配的捕获组；无捕获组时返回整个匹配。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `pattern` | string | 是 | — | Lua 模式 | 长度上限 8 KiB |
| `init` | integer | 否 | `1` | 起始搜索位置 | 不能为 0；负数从末尾计算 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `captures...` | 若干值 | 各捕获组内容 | 无捕获组时返回整个匹配；无匹配返回 `nil` |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `string.match`

---

### `gmatch`

- **方法作用**：返回迭代全部匹配的迭代函数，每次产出捕获组（无捕获组时产出整个匹配）。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `pattern` | string | 是 | — | Lua 模式 | 长度上限 8 KiB；最多 10,000 次匹配 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `iterator` | function | 迭代函数 | 每次调用返回一组捕获 |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `string.gmatch`

---

### `gsub`

- **方法作用**：全局替换匹配内容，返回结果与替换次数。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `pattern` | string | 是 | — | Lua 模式 | 长度上限 8 KiB |
| `repl` | string / table / function | 是 | — | 替换内容 | 字符串中 `%1..%9` 引用捕获、`%%` 转义；表按首个捕获（或整体匹配）取键；函数接收各捕获组并返回字符串/数字/`false`/`nil` |
| `limit` | integer | 否 | 全部 | 最大替换次数 | 默认 10,000 上限 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `result` | string | 替换结果 | 结果超 1 MiB 报错 |
| `count` | integer | 实际替换次数 | — |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `string.gsub`

---

### `regex_find`

- **方法作用**：用正则查找首个匹配，返回位置与捕获表。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `pattern` | string | 是 | — | 正则表达式 | 长度上限 8 KiB |
| `init` | integer | 否 | `1` | 起始搜索位置 | 不能为 0；负数从末尾计算 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `start` | integer | 匹配起点（字符位置，1 起始） | 未匹配时整体返回 `nil` |
| `finish` | integer | 匹配终点（字符位置） | — |
| `capture_table` | table | 捕获组表 | 按 `1..n` 索引，未匹配的组缺失 |

- **方法的使用**：

```lua

```

---

### `regex_match`

- **方法作用**：用正则提取首个匹配的捕获组；无捕获组时返回整个匹配。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `pattern` | string | 是 | — | 正则表达式 | 长度上限 8 KiB |
| `init` | integer | 否 | `1` | 起始搜索位置 | 不能为 0；负数从末尾计算 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `captures...` | 若干值 | 各捕获组内容 | 无捕获组时返回整个匹配；无匹配返回 `nil` |

- **方法的使用**：

```lua

```

---

### `regex_gmatch`

- **方法作用**：返回用正则迭代全部匹配的迭代函数。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `pattern` | string | 是 | — | 正则表达式 | 长度上限 8 KiB；最多 10,000 次匹配 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `iterator` | function | 迭代函数 | 每次调用返回一组捕获 |

- **方法的使用**：

```lua

```

---

### `regex_gsub`

- **方法作用**：用正则全局替换，返回结果与替换次数。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `pattern` | string | 是 | — | 正则表达式 | 长度上限 8 KiB |
| `repl` | string / table / function | 是 | — | 替换内容 | 字符串中 `$0..$9` 引用捕获、`$$` 转义；其余同 `gsub` |
| `limit` | integer | 否 | 全部 | 最大替换次数 | 默认 10,000 上限 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `result` | string | 替换结果 | 结果超 1 MiB 报错 |
| `count` | integer | 实际替换次数 | — |

- **方法的使用**：

```lua

```

---

### `regex_test`

- **方法作用**：判断文本是否匹配给定正则。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `pattern` | string | 是 | — | 正则表达式 | 长度上限 8 KiB |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `matched` | boolean | 是否存在匹配 | — |

- **方法的使用**：

```lua

```

---

### `regex_split`

- **方法作用**：按正则分割字符串，返回数组表。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 目标字符串 | — |
| `pattern` | string | 是 | — | 正则表达式 | 长度上限 8 KiB |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `parts` | table | 分割结果数组 | 最多 10,000 项；总长度超 1 MiB 报错 |

- **方法的使用**：

```lua

```

---

### `format`

- **方法作用**：按格式串格式化值列表。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `format_string` | string | 是 | — | 格式串 | 支持 `%s %q %d %i %u %x %X %o %f %e %E %g %G %c` 与 `%%` 转义 |
| `values` | table | 否 | `nil` | 参数值数组 | 按位置依次填充占位符；缺值或类型不符报错 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `result` | string | 格式化结果 | 结果超 1 MiB 报错 |

- **方法的使用**：

```lua

```

- **等价于原版**：Lua 5.4 `string.format`（部分格式子集）

---

### `rich_text_to_plain_text`

- **方法作用**：将富文本转换为可见的纯文本。
- **方法要求**：无
- **方法参数**：

| 参数名 | 类型 | 必填 | 默认值 | 说明 | 额外补充 |
| ------ | ---- | ---- | ------ | ---- | -------- |
| `text` | string | 是 | — | 富文本字符串 | 可为 `f%` 开头 |
| `rich_params` | table | 否 | `nil` | 富文本参数表 | 键值均为字符串/数字/布尔，用于解析 `f%` 标签 |
| `strip_header` | boolean | 否 | `true` | 是否剥离 `f%` 头 | 为 `false` 时按原样处理 |

- **方法返回**：

| 返回值名 | 类型 | 说明 | 额外补充 |
| -------- | ---- | ---- | -------- |
| `text` | string | 可见纯文本 | 已剥离富文本标记 |

- **方法的使用**：

```lua

```
